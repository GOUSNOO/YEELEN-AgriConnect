# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

YEELEN AgriConnect — full-stack agricultural management app (French UI/comments/domain vocabulary). React frontend (`src/`) + Express/PostgreSQL backend (`server/`), built and deployed as separate services (see Docker section).

Modules: tableau de bord, calendrier, récoltes, cultures & irrigation (capteurs simulés), poulailler (stocks/ventes/livraisons/suivi), pisciculture (mirroir du poulailler — bassins/stocks/ventes/livraisons/suivi, voir ci-dessous), contacts (clients + fournisseurs unifiés — voir le modèle de données ci-dessous), finances, banques, devis/factures (with e-signature), salariés (RH complet : contrats, présences, congés en self-service, avances, référentiels), équipements (inventaire matériel + maintenance), MFA (TOTP + code email), planning (per-culture intervention plan generation, see below), observations (field notes), feedback, aide, recherche globale (Ctrl+K). UI bilingue fr/en (i18n) + affichage montants/dates localisé par entreprise — voir la section « Internationalisation » plus bas.

## Project roadmap (cahier des charges)
Source document: `Feuille_de_route_YEELEN_AgriConnect.pdf` (not in the repo — shared out-of-band, v1.0). Methodology: Agile (Epics/User Stories/MoSCoW/sprints) for product scope, ITIL (service strategy/design/transition/operation/continual improvement) for how the running service is governed. Long-term vision (10-20 years): a full agricultural ERP (crops, livestock, fish farming, stock, equipment, HR, accounting, sales, logistics, transformation, BI, precision agriculture) — deliberately **not** planned in detail now; only the MVP is actively scoped. Business goal at 6 months: ship a free, limited MVP to real farmers with a feedback forum, and let real usage drive the backlog rather than guessing.

**MVP scope (MoSCoW), 6-month horizon:**
- **Must have**: multi-tenant auth+roles, Cultures (parcelles/capteurs), Poulailler, Finances base + auto-sync, Clients, Salariés w/ login, Banques, synced dashboard — per the doc, all already built — **plus**, not yet done: automated DB backups + HTTPS, a chosen/configured hosting target. A feedback forum (simple submission form, not a public community forum) and minimal user docs (a static in-app "Aide" page) are now built — see the dedicated sections further down.
- **Should have**: wider stock mgmt (semences/engrais/phyto), simple equipment inventory, richer HR (présences/congés/avances), simple invoicing (devis/factures — largely already built, see Devis flow above), basic audit log, MFA.
- **Could have**: pisciculture module, exportable reports, basic weather integration.
- **Won't have (this version)**: multi-langue, multi-devise, voice synthesis, precision agriculture (IoT/drones/satellite), agrifood transformation/HACCP, offline mobile app, co-ops — all deferred to the long-term backlog. **Partly overtaken (2026-08-28):** i18n plumbing (fr/en UI via `react-i18next`) + per-entreprise currency/locale *display* formatting are now built (see « Internationalisation » below). Multi-**devise** in the sense the doc meant it — actual currency conversion — is still won't-have; one entreprise = one devise, only the rendering is localised.

**4 milestones (Jalons) after the already-done Jalon 0 (socle)**: Jalon 1 *Consolidation/sécurisation* (backups, hosting, HTTPS, MFA, audit log, full manual E2E testing) → Jalon 2 *Elargissement fonctionnel* (stocks, matériel, RH, facturation) → Jalon 3 *Préparation mise en production* (docs, forum, bug-hardening, user testing) → Jalon 4 *Mise en production* (deploy, open to first farmers, collect feedback).

**Reconciliation against the actual codebase (done 2026-08-12) — the doc is not fully in sync with what's already built:**
- **Ahead of the doc**: MFA (listed as not-done in the doc's security checklist, a Jalon 1 task, and a Should-have) is **already implemented** — `server/src/routes/mfa.js` (setup/verify/resend/disable) + a full frontend flow in `ProfilModule`. Two per-user methods since 2026-08-29: **TOTP** (otplib, QR code) and **code par email** (derived-not-stored HOTP, `server/src/utils/mfaCode.js` — inspired by the reference ERP's `auth_totp_mail`); each user picks their method at enrolment, there is no company-level 2FA setting (the old broken `/mfa/company-method` card was removed). SMS deferred (paid provider). See `docs/journal.md` 2026-08-29. Automated DB backups (listed as blocking/not-done in the doc) are **already implemented** — see the `backup` Docker service above. Neither has been audited for completeness (e.g. is MFA actually *enforced* everywhere it should be — see the deferred enforcement item below; are the backups ever test-restored) — treat as "built, not verified," not "done."
- **Behind the doc's checkmark**: the doc checks off "☒ Variables sensibles hors du dépôt Git (.env / .gitignore)" as done — this was only true for `server/.env`; the repo-root `.env` (holding `DB_PASSWORD`, `EMAIL_PASS`, etc.) was untracked but **not** gitignored until fixed 2026-08-12 (added `.env` to `.gitignore`; it had never actually been committed, so no history cleanup was needed).
- **Not mentioned in the doc but partially built**: an audit trail exists across two tables — `mouvements_historique` (+ `mouvementHistorique.js`) logs edits/deletes on ventes/achats mouvements with before/after values and a stated reason; `audit_log` (+ `auditLog.js`, added 2026-08-13) logs login attempts (success/failure/MFA/unknown-account, with IP/user-agent), readable via `GET /api/auth/audit-log` (admin-only). Sensitive actions beyond logins and ventes/achats mouvements (role changes, MFA disable, devis edits, etc.) still aren't covered, so it's a fuller but still partial start on the doc's "Journal d'audit" item.
- **Confirmed genuinely missing, matches the doc**: HTTPS/TLS (`nginx.conf` itself is still a bare SPA-fallback config, no cert handling — but see the Caddy reverse-proxy setup added 2026-08-13, below, which handles TLS *in front of* it without touching it), hosting decision (host chosen — DigitalOcean — but no account/Droplet/domain provisioned yet). (Feedback forum and user-facing documentation both built 2026-08-15, see dedicated sections below.)

**Jalon 1 work-in-progress (started 2026-08-12).** Hosting/HTTPS: approach and host decided with the user 2026-08-13 (VPS + the existing docker-compose stack + Caddy for automatic HTTPS, on DigitalOcean) and all the deployable config is ready (see dedicated section below) — but actually going live still needs the user to create the DigitalOcean account/Droplet and buy a domain, neither of which can be done on their behalf. Status of the rest:
- ✅ Backups: verified restorable, and a real silent-failure bug found+fixed (see Backups section above).
- ⏸️ MFA enforcement (by-policy): whether an entreprise can *force* 2FA on all its users is still not built — user deferred this to just before mise en production (2026-08-13); don't resume proactively. NB: the login branch itself *is* now proven to gate correctly — as of the 2026-08-29 email-2FA work, a real HTTP e2e test confirmed `POST /api/auth/login` returns `mfaRequired` (no token) when `mfaCode` is missing and `401` when it's wrong, for **both** TOTP and email methods. What remains deferred is the org-wide *enrolment mandate*, not the per-request check.
- ✅ Audit log extension — logins + account/security-relevant admin actions done (2026-08-13); devis financial-workflow actions done (2026-08-29, see below). Design decision made: a new generic `audit_log` table (not an extension of `mouvements_historique`, which is FK'd to `mouvement_id` and shaped around ventes/achats before/after values — doesn't fit login/account events). Schema: `id, entreprise_id (nullable), user_id (nullable), email, action, ip_address, user_agent, details jsonb, created_at`, nullable entreprise/user because failed logins against an unknown email have neither. `server/src/utils/auditLog.js` (`logAuditEvent`/`getAuditLog`) is the shared logger; `GET /api/auth/audit-log` (admin-only, scoped by `entreprise_id`) reads it back. Actions logged so far:
  - `POST /api/auth/login` (all branches): `login_failed_unknown_email`, `login_failed_password`, `login_mfa_required`, `login_failed_mfa`, `login_failed_no_entreprise`, `login_success`. Also `mfa_email_code_sent` (added 2026-08-29) whenever an email 2FA code is dispatched — doubles as the rate-limit counter (5/hour on send; 5/hour on `login_failed_mfa` for verify), shared with `POST /api/mfa/setup` + `/mfa/resend`.
  - `POST /api/mfa/disable`: `mfa_disabled` — a user weakening their own account's security. (`/mfa/setup` and `/mfa/verify` both take `{ method: 'totp' | 'email' }` since 2026-08-29.)
  - `POST /api/salaries` (only the `createAccount` branch), `DELETE /api/salaries/:id` (only when the employee had a linked login): `account_created` (with target user id/email/role in `details`) and `account_deactivated` (target user id in `details`) — for both, `userId`/`email` on the row is the *actor* (the admin), not the target.
  - `PUT /api/entreprise`, `PUT /api/entreprise/banque-principale`: `entreprise_updated`, `banque_principale_updated`.
  - `PUT /api/salaries/:id`: fixed 2026-08-13, not just logged — a real bug, not only an audit gap. `salaries.statut` and `entreprise_utilisateurs.statut` (login access) were two independently-settable fields that happened to share a name; `GET /salaries` filters out `statut = 'Inactif'` (same signal `DELETE /salaries/:id` uses to deactivate the linked account), so a direct `PUT` with `{ statut: 'Inactif' }` could make an employee disappear from every list while their login stayed fully active — no UI path exercised this (`updateSalarie`/`PUT` is imported in `App.jsx` but never actually called; only reachable via direct API calls), but the route was live. Now `PUT` compares old vs. new `statut` and, when a linked `user_id` exists, mirrors `DELETE`'s behavior in both directions: transitioning *to* `'Inactif'` deactivates `entreprise_utilisateurs` + logs `account_deactivated`; transitioning *from* `'Inactif'` reactivates it + logs `account_reactivated` (`details.via: 'PUT /salaries/:id'` distinguishes it from the `DELETE` path's identical action name).
  - Verified end-to-end via real HTTP calls against throwaway test entreprises/admins/employees (cleaned up after each) — all logged actions above, plus the `PUT`-triggered deactivate/reactivate cycle, confirmed correct.
  - `devis.js` financial-workflow actions — **now covered (2026-08-29)**, was the last identified gap of this Jalon 1 item. Every admin-gated devis route logs an `audit_log` row (in addition to its existing `logFieldChanges` per-record journal, which is a different table/purpose): `POST /:id/valider-manuel` → `devis_valide_manuel`, `POST /:id/annuler` → `devis_annule`, `POST /:id/facturer` → `devis_facture` (details: total/modePaiement/modalitePaiement/nbEcheances), `POST /:id/remettre-brouillon` → `devis_remis_brouillon`, `POST /:id/echeances/:echeanceId/payer` → `devis_echeance_payee` (details: echeanceId/montant/nouveauStatut), `PATCH /:id/lignes-quantites` → `devis_quantites_ajustees`. All carry `{ devisId, numero, ... }` in `details` and the *actor* admin as `userId`/`email`. `POST /:id/envoyer` left out on purpose (not admin-gated, not a financial state transition). Verified end-to-end via a throwaway entreprise run through the whole devis lifecycle + `GET /api/auth/audit-log` assertions (17 checks), data cleaned up after. See `docs/journal.md` 2026-08-29.
- 🔄 Manual E2E testing pass (2026-08-13) — done at the **API level only** (no Chrome/browser-automation extension connected this session, so no real UI/click-through pass; that's still outstanding, see below). Exercised every route module against two throwaway multi-tenant entreprises (admin + ouvrier accounts each), verifying golden paths, `requireRole('admin')` gating, and cross-tenant isolation, then cleaned up all test data. Found and fixed four real bugs, none of them caught by the existing Jest suite (still green after, `server/npm test`):
  - **`POST /api/banques` was completely broken** (every call 500'd) — `routes/banques.js` passed `req.user.id` (doesn't exist on the JWT payload; only `sub`/`entrepriseId`/`email`/`role` do) instead of `req.user.entrepriseId` into `banquesService.createBanque`, hitting `entreprise_id`'s `NOT NULL` constraint. No bank account could be created via the API at all. Fixed by passing `req.user.entrepriseId`, matching the `PUT`/`DELETE` handlers in the same file, which already did this correctly.
  - **`POST /api/achats` and `GET /api/achats` (list) were completely broken** (both 500'd) — `DOCUMENT_COLUMNS` in `routes/achats.js` prefixed every column with an `ad.` alias (needed only by `getDocumentComplete`'s own aliased query), but got reused verbatim in an `INSERT ... RETURNING` and in a `SELECT ... FROM achats_documents` with no alias declared — both raise "missing FROM-clause entry for table ad". The entire multi-line achats-document feature (a Should-have MVP item) could never create or list a single row via the API. Fixed by dropping the alias prefix from `DOCUMENT_COLUMNS`, matching the alias-free convention every other `*_COLUMNS` constant in the codebase already uses specifically so it's safe in both `SELECT` and `INSERT ... RETURNING` contexts.
  - **`DELETE /api/achats/:id` had a cross-tenant deletion hole** — it deleted `achats_lignes WHERE document_id = $1` (no `entreprise_id` scoping, since that table has none of its own) *before* confirming the parent document belonged to the caller's entreprise. Any authenticated user of any company could delete another company's purchase-document line items just by guessing/knowing a valid document id — the parent document would survive (untouched, so the response looked like a harmless 404) but silently lose its lines. Fixed by checking ownership first, then deleting lines only if the document actually belongs to the caller.
  - **`observations` table didn't exist at all** — confirmed via `\d observations` in the running DB; matches the already-documented gap (`server/src/db/migrate.js` never creates it, and `migrations/001_create_observations_table.sql` — the only schema source — isn't wired to any runner). The entire Observations module 500'd on every call. Also fixed the FK bug flagged in the Known Issues section while there (`REFERENCES utilisateurs(id)` → `REFERENCES users(id)`, the real table name), then applied the corrected migration by hand (`psql -f` inside the `db` container) to unblock testing. **Process gap closed 2026-08-13**: the (already-FK-fixed) `CREATE TABLE observations` statement is now folded directly into `server/src/db/migrate.js`, so any environment that runs the documented migration command gets this table automatically — no more manual `psql -f` step needed. `migrations/001_create_observations_table.sql` itself is now redundant (superseded, not deleted — harmless to leave as a historical record since nothing references it).
  - Also fixed in passing (found while reading, not exercised into a failure first): `banquesService.updateBanque`'s `RETURNING` clause returned raw snake_case (`nom_banque`, `type_compte`) instead of the camelCase aliasing `createBanque`/`getBanque` use — harmless today since `updateBanque` (like `updateSalarie`) is defined in `src/lib/api.js` but never actually called from the UI, but would silently break an edit-bank-account feature the moment one gets built on top of it.
  - **Fixed 2026-09-05**: `POST /api/devis/:id/envoyer` used to update `devis.statut` to `'Envoyé'` and generate/store the public `token_public` *before* calling `sendDevisEmail`; if the send failed, the route 500'd but the devis was left stuck in `'Envoyé'` with a live public link nobody knew about. Fixed by moving the email send *before* any DB write (same pattern as `mfa.js`'s email-code send): the token is built in memory to construct the link, the email is sent, and only on success is `statut`/`token_public` persisted + the field-change journal written. A send failure now returns `502` (`"L'email n'a pas pu être envoyé. Le devis reste en brouillon, réessayez."`, matching `mfa.js`'s `502` convention for a failed email-code send) with the devis left exactly as it was — no more manual `remettre-brouillon` recovery needed. 2 new tests in `devis.test.js` (email failure → 502 + `statut` still `'Brouillon'` + `token_public` still `NULL`; client without an email → 400, no send attempted).
  - **Fixed 2026-08-13**: `/api/business/finances` `POST`/`DELETE` had no `requireRole` gate at all, unlike every other admin-sensitive route — confirmed a token with role `ouvrier` could create and delete finance entries via direct API calls despite the frontend hiding that UI from it. Per explicit user decision, now gated `requireRole('admin', 'directeur')` — only those two roles (see the new `directeur` role above) may write finance entries; every other role is read-only there. `clients`/`fournisseurs` in the same file were deliberately left ungated — `comptable`'s whole stated purpose is "suivi client," so locking client writes to admin/directeur would conflict with that role's design; revisit only if that turns out to be wrong.
  - **Fixed 2026-08-30 (same pattern)**: `/api/banques` `POST`/`PUT`/`DELETE` had no `requireRole` either — an `ouvrier` could create/edit/delete bank accounts via direct API calls, inconsistent with the finance-entries gate above (bank accounts being what those entries reference). Per explicit user decision, now gated `requireRole('admin', 'directeur')` (`GET /` stays open to all roles). Also fixed while there: `DELETE /api/banques/:id` returned `{ success: true }` / 200 and `PUT /api/banques/:id` returned `{ banque: null }` / 200 even on 0 rows (wrong tenant / bogus id) — both now 404, matching `PUT /api/contacts/:id` and the 2026-08-30 `DELETE /api/contacts/:id` fix. `banquesService.deleteBanque` now returns `rowCount > 0` instead of always `true`.
  - Full module coverage this pass: auth/roles/tenant isolation, Cultures (parcelles + vanne historique + mouvements + finance sync + mouvement historique), Poulailler (stocks/mouvements/livraisons/suivi), Finances/Banques, Clients/Fournisseurs, Devis (brouillon→signé→facturé échelonné→paiement d'échéances→remise en brouillon, plus the public token view/sign/PDF routes), Achats, Salariés, Observations, Planning.
  - **Real browser click-through pass — done 2026-08-13** (Chrome extension connected, throwaway entreprise "E2E Browser Test SARL" registered, exercised, then fully deleted — see cleanup note below). Confirmed the App.css/corruption-recovery fixes from earlier the same day render correctly (dashboard, nav chips, all module layouts checked visually, no leftover Tailwind/meta-commentary artifacts). Found and fixed:
    - **`AchatModule`'s multi-line achat form (Cultures/Poulailler → Achats tab) 500'd on every submission from the UI**, despite the `DOCUMENT_COLUMNS` alias bug above being fixed — a *different*, previously-masked bug: `App.jsx`'s `submitForm` sent `date: new Date().toLocaleDateString('fr-FR')` (`"13/08/2026"`, day-first) straight into `POST /api/achats`, which inserts it unparsed into a Postgres `date` column. Postgres's default `DateStyle` reads `DD/MM/YYYY` as month-first, so any day-of-month > 12 threw `date/time field value out of range` (confirmed via `docker logs`); days ≤ 12 would have silently swapped day and month instead of erroring — a live, ongoing bug that a hand-crafted API-level test (using ISO dates directly) could never have caught, which is exactly why this browser pass mattered. Fixed by sending `new Date().toISOString().slice(0, 10)` instead (`App.jsx` ~line 1179). Side effect surfaced by the fix: the achats-history table then displayed the raw ISO timestamp instead of a formatted date — fixed by wrapping it in the existing `formatDateFr` helper (already used identically by four other tables in the file), matching convention instead of inventing a new one.
    - **The Observations module had zero UI entry point** — `ObservationListView` (component + backend both already fixed/tested per the entries above) was imported in `App.jsx` but never rendered anywhere, and `'observations'` appeared nowhere in `availableTabs` or any role's `permissions` list. Nobody, in any role, could reach it. Per explicit user decision, added a plain `{ id: 'observations', label: 'Observations', icon: ClipboardList }` tab — ungated by role/`activated` module toggle, same treatment as `Profil` — plus the `{tab === 'observations' && <ObservationListView />}` render line. Verified end-to-end (create/list an observation, backend row confirmed).
    - **`POST /api/planning` had the same "zero frontend caller" shape as Observations had — fixed 2026-09-05** (see `services/` above for the full description): real per-culture calendar, persisted as `activites`, wired into a "Plan d'intervention" section on each parcelle card in `CulturesModule`.
    - **Calendrier and Récoltes were 100% client-side (`localStorage`), not shared team data** — unlike every other module, they never called the backend at all (`storageGet`/`storageSet` only, keyed by the logged-in user's email). Two employees of the same entreprise on different machines would each see their own private calendar/harvest log, silently breaking the multi-tenant model the rest of the app follows. Root-caused, planned, and fixed in one pass (see dedicated entry below).
  - **Cleanup**: the throwaway test entreprise (`entreprises.id = 14`, "E2E Browser Test SARL") and its two users (`e2e-browser-test@yeelen.test` admin, `fatoumata.kone@yeelen.test` employee) were fully deleted afterward. Needed manual multi-table cleanup, not just `DELETE FROM entreprises` — most tables cascade on `entreprise_id` (`achats_documents`, `audit_log`, `banques`, `calendar_events`, `devis`, `entreprise_utilisateurs`, `fournisseurs`, `mouvements_historique`, `observations`, `salaries`), but a good number don't (`clients`, `cultures`, `cultures_mouvements`, `finances`, `lots_volailles`, `parcelles`, `parcelles_historique`, `poulailler_*`, `poulaillers`, `production_oeufs`, `recoltes` are all `NO ACTION`) — worth knowing before assuming a straightforward `DELETE FROM entreprises WHERE id = ...` will ever fully remove a company's data.


### Historique du projet

Le journal de développement daté (correctifs, décisions techniques et chantiers livrés
depuis le 2026-08-13) vit dans `docs/journal.md`. À consulter en cas de doute sur le
pourquoi d'une décision passée, ou avant de retoucher un module déjà repris.

## Commands

Frontend (repo root):
```bash
npm install
npm run dev       # Vite dev server
npm run build     # production build to dist/
npm run preview
npm test          # jest + jsdom, JSX via babel.config.cjs (frontend tests only — server/ has its own, see below)
```

Backend (`server/`):
```bash
cd server
npm install
npm run dev        # nodemon src/server.js
npm start          # node src/server.js
npm test           # jest unit tests (own babel.config.cjs — separate from the frontend's, don't merge them)
npm run test:integration   # supertest against the real app; needs `docker compose up -d db` (see Testing section)
node src/db/migrate.js   # apply the backend's idempotent migration script (run from server/)
```

Docker (full stack — see `docker-compose.yml`):
```bash
docker-compose up
```
Spins up: `db` (postgres:18-alpine), `backend` (server/, port 4000), `frontend` (Vite build served by nginx, port 8090), `backup` (periodic `pg_dump` sidecar, see below). Frontend's `VITE_API_URL` is baked in at build time via a Docker build arg.

There is no lint script wired into either `package.json` despite `.oxlintrc.json` existing at the root; run `npx oxlint` directly if linting is needed.

### CI
`.github/workflows/ci.yml` (added 2026-08-30) runs on every push + PRs to `main`, two jobs:
- **backend**: spins up a `postgres:18-alpine` service, `npm ci` in `server/`, then `npm test` (unit) + `npm run test:integration` (env overrides `TEST_DB_PORT=5432` / `DB_USER=DB_PASSWORD=postgres` / `JWT_SECRET=ci-test-secret` — no GitHub secret, the DB is an ephemeral CI container). `globalSetup` migrates a fresh `agri_app_test` there, so CI is also the from-scratch-schema guard for the `migrate.js` drift described above.
- **frontend**: `npm ci --legacy-peer-deps` (root), `npm test` + `npm run build`.

## Architecture

### Multi-tenant model
Data is partitioned by **company (`entreprise`)**, not by individual user. Key tables: `users`, `entreprises`, and the join table `entreprise_utilisateurs` (`user_id`, `entreprise_id`, `role`, `statut`) — a user's role is scoped per-company, not global. `server/src/routes/auth.js` shows the full lifecycle: `register` creates a `users` row + an `entreprises` row + the linking `entreprise_utilisateurs` row in one transaction; `login` looks up the active `entreprise_utilisateurs` row to determine role/company.

- JWT payload: `{ sub, email, entrepriseId, role }`, signed/verified with `env.JWT_SECRET`.
- `authRequired` (`server/src/middleware/auth.js`) verifies the JWT and sets `req.user`.
- `requireRole(...roles)` (`server/src/middleware/requireRole.js`) gates routes by `req.user.role` (e.g. `requireRole('admin')` on destructive/admin-only actions in `devis.js`, `entreprise.js`, `salaries.js`).
- Frontend roles (`src/components/roles.js`, `ROLE_DEFINITIONS`): `admin`, `directeur`, `gestionnaire`, `comptable`, `assistant_direction`, `ouvrier` — each maps to a fixed list of allowed UI sections (permissions array), independent of backend `requireRole` checks. `directeur` (added 2026-08-13) mirrors `admin`'s full permission set; `assistant_direction` (added same day) mirrors `comptable`'s scope plus `fournisseurs`. `role` is a free-text column with no DB-level enum/CHECK constraint, so these are a convention enforced only by the frontend dropdown (`App.jsx`'s `EmployeesModule`, the account-creation role `<Select>`) and `requireRole(...)` allowlists on the backend — not a real constraint. README's access matrix predates this and still only lists the original 4 roles; update it if it becomes a source of confusion. This 6-role set is the fixed-roles half of a two-part plan; the other half — letting each entreprise define its own custom roles/permissions — was explicitly scoped out as a separate, much larger effort (a real per-entreprise permissions engine) and is not started.
- **Every route handler scopes its queries by `req.user.entrepriseId`** — this is the actual tenant-isolation boundary; there is no other enforcement layer (no RLS, no ORM-level scoping). When adding a table/route, mirror the existing pattern in `business.js`/`cultures.js`/`poulailler.js` (`WHERE entreprise_id = $1`, insert with both `entreprise_id` and `user_id`).

### Unified data model (ERP alignment, 2026-08-18) — read before touching contacts/produits/prix
Three earlier split tables were merged as part of aligning the schema on a reference ERP. Full rationale + migration details in `docs/journal.md` (2026-08-18 entries); the essentials:
- **`contacts`** replaces the separate `clients` / `fournisseurs` tables. One row per partner, with a `type` (`client` / `fournisseur` — a contact can be both), company-vs-particulier shape, sub-contacts, and tags (`contact_tags` + `/api/contact-tags`). `GET /api/contacts?type=client|fournisseur` filters. Deleting a contact still referenced by a devis → `409`. The frontend still has distinct **Clients** and **Fournisseurs** tabs, both backed by `/api/contacts`.
- **`produits`** replaces `cultures_stocks` / `poulailler_stocks`. One catalogue, per-row `categorie` (CRUD via `/api/produit-categories`), `prix_defaut`, stock quantity. The Cultures/Poulailler/Pisciculture "Stocks" tabs all read `/api/produits` (filtered by category grouping), and the stock↔achats/ventes sync resolves rows by `produit_id` first, name-match as fallback. **Module Pisciculture (2026-09-05)**: `'Pisciculture'` was added as a third value to the `module` `CHECK` constraint (`produits`, `produit_categories`, `produit_templates`, `achats_documents` — 4 places in `migrate.js`, `DROP CONSTRAINT IF EXISTS`/`ADD CONSTRAINT` idempotent pattern) plus every literal `['Cultures', 'Poulailler']` whitelist in route validation (`produits.js`, `produitCategories.js`, `produitTemplates.js`, `achats.js` ×4) and in `utils/stockSync.js`'s `applyAchatLignesToStock`/`reverseAchatLignesFromStock` (these silently no-op for an unlisted module — no error, just a stock that never syncs, the actual bug class this touches). Built as a structural mirror of Poulailler (see `PiscicultureModule` in `App.jsx`): `StocksTab`/`VentesWithDevis`/`AchatModule`/`ComptabiliteTab` reused as-is with `moduleType="Pisciculture"`; only `BassinsEnvironnementTab` (water-quality gauges: pH/oxygène dissous/température, no backend — pure client simulation, same as `EnvironnementTab`), `PiscicultureMonitoringTab` and `PiscicultureLivraisonsTab` are dedicated components, backed by new `pisciculture_livraisons`/`pisciculture_suivi` tables and `routes/pisciculture.js` — deliberately **not** mirroring Poulailler's own `poulailler_mouvements` ledger, confirmed dead (no frontend caller) and superseded by the devis/achats_documents flow.
- **`listes_prix`** (+ `listes_prix_lignes`) replaces the old per-contact `client_prix`: named, reusable price lists assigned to a contact; `GET /api/listes-prix/prix-effectifs?contactId=…` resolves the effective price (list line → else `produits.prix_defaut`). Detaching a list from a contact is `ON DELETE SET NULL`.

### Internationalisation (i18n, étape 1 — 2026-08-28)
Two independent axes, deliberately separated (details in `docs/journal.md` 2026-08-28):
- **UI language** — `react-i18next`, `src/i18n/index.js`, catalogues `src/i18n/locales/{fr,en}.json` (imported directly — 2 langs, negligible bundle cost). **Per-user** choice, persisted in `localStorage` (`agri-lang`), browser-lang fallback then `fr`. Components read strings via `const { t } = useTranslation()`. `LANG_EXPLICIT_KEY` marks that the user picked a language via the Profil selector — until then, login may align the UI language to the entreprise's locale.
- **Currency/date display** — `src/lib/locale.jsx`, **per-entreprise** (`devise` + `locale`, default `XOF` / `fr-FR`). `useLocale()` hook exposes `fmtMoney` / `fmtDate` / `fmtNumber`; standalone `fmt*` / `fmt*With` helpers work outside components (module-level `_config` kept in sync by `LocaleProvider`). No currency **conversion** — formatting only. Blank → `—`.
- Most `App.jsx` module views and the extracted components have been migrated to `t(...)`; new user-facing strings must go through `t(...)` + both json files, not hardcoded French.

### RH module (complet — 2026-08-27/28)
`/api/rh` (référentiels: départements, postes, types de contrat — writes gated `requireRole('admin')`) plus the RH surface in `salaries.js`. Frontend: `EmployeesModule` (in `App.jsx`), `EmployeeRhModal.jsx` (per-employee détail: contrats — one-active invariant, présences, congés, avances), `MonEspaceRh.jsx` (**self-service**: an employee requests their own congé, cannot self-approve — an admin/directeur does; `conges-solde` decrements on approval), `RhReferentiels.jsx` (référentiel CRUD, `canManage`-gated).

### ERP UX layer (2026-08-26/27)
- **Global search** — `GlobalSearch.jsx` (Ctrl+K), min 2 chars, debounced `/api/recherche`, grouped results (contacts / devis / produits / …).
- **Devis Kanban** — `DevisModule` renders a drag-and-drop board; valid transitions gated by `isValidDevisTransition`. Plus per-devis "activités planifiées" (`/api/activites`, a generic activity model attachable via `ressourceType`/`ressourceId`, also used on contact détail) and a message thread / chatter (`/api/messages`).
- **URL routing** (phase 1) + adaptive tab bar in the root shell.

### Backend structure (`server/src/`)
- `server.js` — thin entrypoint (`testDatabase()` + `listen()`); the Express app itself is the factory `server/src/app.js` (recreated 2026-08-29, shared with the integration test suite). It mounts routes flatly under `/api/*`: `auth`, `business`, `cultures`, `poulailler`, `entreprise`, `salaries`, `banques`, `mfa`, `devis`, `achats`, `observations`, `planning`, `calendar`, `recoltes`, `feedback`, `equipements`, `produits`, `produit-categories`, `contacts`, `contact-tags`, `listes-prix`, `payment-terms`, `taxes`, `journals`, `accounts`, `factures`, `paiements`, `recherche`, `activites`, `messages`, `rh`. Each route file inlines its own `pg` queries directly — no ORM, no repository layer, no shared query builder.
- `db.js` — the single `pg` `Pool` instance every route imports; it reads DB config straight from `process.env` via its own `dotenv.config()` call. `config/env.js` separately loads the *repo-root* `.env` for `JWT_SECRET`/`PORT`. There's no single shared env-loading entrypoint — check which of the two a given file needs.
- `services/` — cross-route business logic: `mailer.js` (nodemailer/Gmail transport: MFA codes, welcome emails, devis emails), `banquesService.js` (bank account CRUD used by `routes/banques.js`), `planningService.js` (`generatePlan`, used by `routes/planning.js`), `cultureService.js` (`getCulturePlanDetails`: looks up a `parcelles` row by id + `entreprise_id`).
- **Fixed 2026-09-05** (was previously a placeholder, see `docs/journal.md`): `cultureService.js` now returns a **real per-culture calendar** — `CALENDRIERS_PAR_CULTURE` (~10 widely-grown crops: Maïs, Riz, Blé, Tomate, Pomme de terre, Oignon, Haricot, Soja, Arachide, Manioc; agronomic day-offsets from semis, written fresh for this project), matched case/accent-insensitively against `parcelles.culture` free text, with the old generic 5-milestone schedule kept as `CALENDRIER_GENERIQUE` fallback for any unrecognized culture. Dates are computed from `parcelles.date_semis` (new nullable column) when set, else from today (unchanged fallback). `POST /api/planning` (`routes/planning.js`) takes `{ cultureId }` — despite the name, `cultureId` is actually a **`parcelles.id`** (there's no separate "culture" table) — and now **persists** each generated milestone as an `activites` row (`ressourceType:'parcelle'`, the existing generic activity model, its `RESSOURCES_VALIDES` allowlist extended to include `'parcelle'`) instead of the old commented-out `savePlanningToDB` TODO. No dedup on repeat calls — a confirm dialog on the frontend button warns before adding more tasks. Frontend entry point added (previously none existed): a collapsible "Plan d'intervention" section inside each parcelle `Card` in `CulturesModule` — a `dateSemis` field (`PUT /cultures/parcelles/:id`, `PARCELLE_COLUMNS` now `to_char`s it to dodge the pg-DATE→JS-Date TZ shift, same fix pattern as `validity_date`/`date_echeance`), a "Générer le plan" button, and `<ActivitesSection ressourceType="parcelle" ressourceId={p.id} />` reused as-is (already generic, previously used only for `'devis'`/`'contact'`). 4 new/updated tests in `planning.test.js` (real per-culture milestones + persistence, `date_semis`-driven dates not "today", unknown-culture fallback, existing 400/404 cases).
- `utils/` — `financeSync.js` (`syncFinanceEntry`/`updateFinanceEntry`/`removeFinanceEntry`/`syncDevisPaiement`: every vente/achat/facture payment writes a mirrored row into `finances`, categorized `Banque` or `Caisse` depending on whether the company has a `banque_principale` set — this is how cross-module financial consistency is maintained, there are no DB triggers), `mouvementHistorique.js` (audit log of edits/deletes on mouvements, written explicitly by routes, read back via `getPoulaillerHistorique`/`getCulturesHistorique`), `devisPdf.js` (pdfkit invoice/quote rendering, streamed to the response).
- `controllers/observationsController.js` exists but is **dead code** — nothing imports it. The live `/api/observations` logic is inline in `routes/observations.js`, scoped by `entreprise_id` from the JWT like every other route (inconsistent with the file's own comment claiming it needs `companyId` support added).
- Devis (quotes/invoices) is the most elaborate flow: `Brouillon → Envoyé → Signé → Facturé/Non payé/Payé partiellement`, with a public token-based route (`/devis/public/:token`, no auth) for client-side viewing + e-signature, PDF generation both for the owner and via public token, and per-échéance (installment) payment tracking that syncs into `finances` only when an installment is actually paid.
- **ERP "Comptabilité" roadmap — Stage 0 (2026-08-30)** started replicating Odoo's `sale`/`account` model (see `docs/journal.md` + the memory `project_erp_comptabilite_roadmap`; all 6 stages done (roadmap complete)). Stage 0 shipped: `devis.validity_date` (default +30d) + a computed `expired` flag (`DEVIS_COLUMNS`, not stored — like Odoo `is_expired`); an `Annulé` devis is now deletable (`DELETE /:id` allows `Brouillon`/`Devis`/`Annulé`, matching Odoo "draft or cancel"); **`payment_terms` + `payment_term_lines`** tables (`account.payment.term`-like: `value` percent/fixed/balance, `delay_type` days_after/days_after_end_of_month, `nb_days`) with CRUD at `/api/payment-terms` (writes gated `requireRole('admin','directeur')`), a default set seeded at `register` + by `migrate.js:seedPaymentTermsForExistingEntreprises`; `POST /devis/:id/facturer` now accepts `paymentTermId` (+ optional `acompte` `{method:'percentage'|'fixed', value}`) to **generate** the `echeances_paiement` from the term instead of hand-entering them (`genererEcheancesDepuisTerme` in `routes/paymentTerms.js`) — the old manual `modalitePaiement`/`echeances` path stays as fallback. Frontend: "Valable jusqu'au" field + "Expiré" badge in `DevisModule`, a "Condition de paiement" selector + acompte in the facturer modal, and a collapsible `PaymentTermsPanel` référentiel. Also fixed here: a pre-existing double-`client.release()` on `facturer`'s early-return paths (`pg-pool` `throwOnDoubleRelease`), and `validity_date`/`date_echeance` are now `to_char`'d to avoid the pg-DATE→JS-Date TZ shift. `npm run test:integration` gained `--forceExit`.
- **ERP "Comptabilité" — invoice form on real Odoo SCSS values, 2026-08-30 (frontend-only).** After the UI-alignment pass below, the `FacturesModule` **detail modal** was rebuilt from dimensions **extracted from the Odoo SCSS source** (`C:\Users\PC\reference-repos\erp-source`, `addons/web/static/src/...`), not estimated — see the `.oe-invoice` block at the bottom of `src/App.css`, which cites the source file for every value. Key facts now matched to the letter: base 14px / lh 1.5 / text #212529 / border #dee2e6 / thead bg #f8f9fa; **statusbar** caret `1em`, inactive #dee2e6, **active = `mix(#71639e,#f8f9fa,20%)` = #dddbe8 with a #71639e side border and text still #212529** (the previous green-fill/white-text was wrong); **`o_inner_group`** grid `fit-content(150px) minmax(0,1fr)` gap `8px 16px`, label 14px weight 400 opacity .66; **`oe_subtotal_footer`** grid `1fr auto` margin-left auto, right-aligned labels + trailing `:`, total row `border-top:1px` `font-weight:700` `font-size:1.3em`; list cells `8px / 4.8px` (16px edges) `border-collapse:collapse`; notebook `.nav-tabs` with active tab's bottom border = sheet white. Scoped to `.oe-invoice` so the app-wide `.data-table` is untouched. Frontend build + 88 tests green.
- **ERP "Comptabilité" — UI alignment pass, 2026-08-30 (frontend-only, no backend/schema/test change).** Reworked every Comptabilité component to the app's existing Odoo-dimensioning discipline (shared `.data-table` class + `.flat-input` + `.field-group` from `project_erp_dimensioning_appwide`, and the devis-modal structure from `project_erp_devis_visual_alignment`): **`FacturesModule`** — list columns in Odoo `account.move` tree order (`Numéro | Client | Date de facture | Échéance (widget remaining_days: « J+n » / « n j de retard ») | Total HT | Total TTC | État paiement | État`); detail modal restructured to the `account.move` form skeleton — chevron `MoveStatusBar` (draft→posted, `Annulée` badge), action bar in Odoo order (Valider · Enregistrer un paiement · Créer un avoir · Retour brouillon · Annuler · Supprimer · Vérifier l'intégrité), two-column `.field-group` header (left Client/Origine/Réf/liens avoir, right Date/Échéance/Relances), notebook tabs **« Lignes de facture »** (`Désignation | Qté | P.U. | Remise % | Taxes | Montant TTC`) and **« Écritures comptables »** (`Compte | Libellé | Débit | Crédit | Lettrage`), `oe_subtotal_footer`-style totals block bottom-right (Total HT · [par taxe] · Total TTC · Payé · Reste dû). `ComptaReportsPanel` / `ComptaConfigPanel` / `TaxesPanel` / `PaymentTermsPanel` — list `<div>`s replaced by `.data-table` with Odoo-report column layouts (aged: Client × notDue/1-30/31-60/61-90/90+/Total + bold totals row; journals: Code/Nom/Type/Sécurisé; accounts: Code/Nom/Type/Rapprochable; taxes: Nom/Type/Montant/Incluse; payment terms: Nom/Répartition). `TaxSelect` → `many2many_tags`-style rounded chips with `×`. Devis line tables: `Taxes` column moved after `Remise` (Odoo SO line order). i18n keys added (`factures.tab.*`, `factures.due*`, `comptaConfig.hash*`, `comptaReports.*`, `paymentTerms.repartition`). Frontend build + 88 tests green.
- **ERP "Comptabilité" roadmap — Stage 6 (balance âgée + relances + paiements autonomes), 2026-08-30 — roadmap COMPLETE.** Schema: `account_move.relance_niveau INT DEFAULT 0` + `.derniere_relance DATE` (reminder tracking; no email — SMTP deferred). New in `accountMove.js`: `creerPaiementAutonome(client, {entrepriseId, userId, partnerId, amount, paymentDate, journalId, ref, sens})` — an `account_payment` + its posted `account_move` (treasury D / receivable C) **without reconciliation** (an unallocated credit on the partner account); `allouerPaiement(client, {paymentId, moveId, amount, entrepriseId})` — reconciles a slice of the payment's `payment_term` line against an invoice's via `lettrerLignesPartenaire`, clamps to `min(demandé, non-alloué, résiduel facture)`, updates the invoice's `payment_state`; same-partner check → 400. Routes: `GET /api/factures/aged-receivable?date=` (per-partner `amount_residual` of posted `out_invoice`/`out_refund`, bucketed *notDue / 1-30 / 31-60 / 61-90 / 90+* vs `invoice_date_due`, `reversed` excluded, `out_refund` negative), `GET /api/factures/overdue` (posted `out_invoice`, `payment_state` in not_paid/partial, past due — with `daysOverdue`), `POST /api/factures/:id/mark-reminded` (admin/directeur — `relance_niveau += 1`, `derniere_relance = today`); new **`server/src/routes/paiements.js`** (`/api/paiements`): `GET /?partnerId=&unallocated=1` (unallocated amount = abs of the payment move's `payment_term` line residual), `POST /` + `POST /:id/allocate` (admin/directeur). aged-receivable/overdue declared before `/:id`. `MOVE_COLUMNS` adds `relanceNiveau`/`derniereRelance`. **`payment_state='in_payment'`** deliberately not used (no bank-statement import in YEELEN — the 4 states stay). Frontend: new `src/components/ComptaReportsPanel.jsx` (collapsible: aged table partner×bucket, overdue list + «Marquer relancé», standalone-payment form + unallocated list + inline «Affecter» picker) rendered atop `FacturesModule`. i18n `comptaReports.*`. Migration rehearsed ×2. New `agedReceivable.test.js` (5) + `paiements.test.js` (7). **144 → 154 integration tests / 26 files.**
- **ERP "Comptabilité" roadmap — Stage 5 (avoirs / `out_refund` + reverse), 2026-08-30.** No schema change (`reversed_entry_id` / `out_refund` / `RINV` sequence all from Stage 3). Extracted **`lettrerLignesPartenaire(client, entrepriseId, {ligneFactureId, ligneContreId, amount, date})`** from `enregistrerPaiementMove` — the partial→full `account_partial_reconcile` / `account_full_reconcile` / `matching_number` logic, now sign-aware on BOTH lines (each residual reduced by `amount` toward 0); shared by payment + reverse. New **`reverseMove(client, {moveId, entrepriseId, userId, reason, date, refundMethod})`**: origin must be `posted` `out_invoice`/`in_invoice`; creates an `account_move` (`out_refund`/`in_refund`, `reversed_entry_id` = origin, `ref` = "Annulation de : <name> — <reason>", `invoice_origin` = origin name), copies its product/section lines + tax links. `refundMethod:'refund'` (default) leaves it **draft** (edit + post later); `refundMethod:'cancel'` calls `posterMove` (sign-flipped entry + `RINV/…` number + hash if journal secured) then `lettrerLignesPartenaire` against the origin's `payment_term` line → origin `payment_state='reversed'` + `amount_residual=0`, credit note `paid`. Route `POST /api/factures/:id/reverse` (admin/directeur). Guards: `enregistrerPaiementMove` → 400 if origin `payment_state='reversed'`; `POST /devis/:id/remettre-brouillon` → 400 if the linked move has any reversal moves (alongside the hash guard). `MOVE_COLUMNS` adds `reversedEntryName` (origin's name) + `reversalMoveNames` (`string_agg` of moves reversing this one). Frontend: `FacturesModule` detail modal — «Créer un avoir» button on a posted `out_invoice` → inline form (`reason` + method select), opens the created credit note; header shows «Avoir de : …» / «Annulée par : …». i18n. Rehearsal ×2 (formality — no schema delta). New `factureAvoir.test.js` (7): `refund` → draft `out_refund` linked + lines copied; `cancel` → `RINV/…` posted balanced (product debit / receivable credit) + origin `reversed` residual 0 + `matching_number` both sides; payment on a `reversed` invoice → 400; reverse of a draft / of a credit note → 400; reverse on a secured journal → credit note hashed + chain still verifies; tenant isolation. **138 → 144 integration tests / 24 files.**
- **ERP "Comptabilité" roadmap — Stage 4 (inaltérabilité du `posted`), 2026-08-30.** **Opt-in per journal**, off by default (so the pre-prod `remettre-brouillon` undo stays usable until an entreprise turns it on). New: `account_journal.restrict_mode_hash_table BOOL` + `account_journal.secure_sequence_last INT` (gap-free counter, row-locked at post), `account_move.inalterable_hash TEXT` + `account_move.secure_sequence_number INT`. `server/src/utils/accountMove.js:hacherMoveSiRequis(client, moveId, journalId)` — when the journal is secured, assigns `secure_sequence_number = last+1` and `inalterable_hash = sha256(prevHash + chaineIntegriteMove(move))` chained to the previous secured move of the same journal; `chaineIntegriteMove` concatenates the protected fields (`name|date|journal_id|amount_total|partner_id` then each line `account_id|debit|credit|balance` ordered by id). Called from `posterMove` (invoice) **and** after the payment entry insert in `enregistrerPaiementMove`. `verifierChaineJournal(q, journalId, entrepriseId)` re-walks the chain: recomputes each hash from the previous and checks `secure_sequence_number` has no gap → `{ ok } | { ok:false, brokenAt, reason:'hash'|'gap' }`. Guards: `POST /factures/:id/button-draft` and `DELETE /factures/:id` → 400 if `inalterable_hash` set; `POST /devis/:id/remettre-brouillon` → 400 if the linked move is hashed (message points to "créez un avoir", Stage 5). `GET /api/factures/verify-hash?journalId=` (admin/directeur, declared before `/:id`). `journals.js` PUT/POST accept `restrictModeHashTable` — but **only to enable it** (`=== true`); a `false` in a partial PUT is ignored (can't un-secure a journal with hashed entries). Also fixed here: 2 pre-existing double-`client.release()` on `remettre-brouillon`'s early returns. Frontend: `ComptaConfigPanel` per-journal 🔒 toggle (`updateJournal({restrictModeHashTable:true})`, confirm dialog, irreversible); `FacturesModule` detail modal shows a «Sécurisée» lock chip + a «Vérifier l'intégrité» button (→ `verify-hash`) when the move is hashed. i18n. Migration rehearsed ×2. New `factureHash.test.js` (5): unsecured journal → no hash + button-draft OK; enable → post assigns hash+seq, chained, button-draft/delete → 400; `false` PUT ignored; `verify-hash` ok then tamper a `debit` directly in DB → `{ok:false, reason:'hash', brokenAt}`; `remettre-brouillon` of a devis linked to a hashed move → 400. **133 → 138 integration tests / 23 files.**
- **ERP "Comptabilité" roadmap — Stage 3b (rebranchement `facturer` → `account.move`), 2026-08-30.** `POST /api/devis/:id/facturer` now creates **and posts** a real `account_move` (`out_invoice`) mirroring the devis — `devis.move_id` links to it (new nullable column), `account_move.invoice_origin` = the devis numéro, échéances are dual-linked (`echeances_paiement.devis_id` AND `.move_id`). The devis keeps its own `statut`/échéance flow as the commercial mirror. Extracted **`server/src/utils/accountMove.js`**: `posterMove(client, moveId, entrepriseId)` (the balanced-entry generation + journal numbering, was inline in `routes/factures.js`) and `enregistrerPaiementMove(client, {…, skipFinanceMirror, skipEcheanceAllocation})` (payment + reconciliation core); `routes/factures.js`'s `/post` and `/register-payment` handlers are now thin wrappers around them (behaviour unchanged, `factures.test.js` still green). `facturer`'s `complet` path calls `enregistrerPaiementMove(…, skipFinanceMirror:true)` so the move settles to `paid`+reconciled while `syncDevisPaiement` remains the single `finances` entry (`source_module='Devis'`). `POST /devis/:id/echeances/:eid/payer` is now transactional and, when `devis.move_id` is set, also runs `enregistrerPaiementMove(…, skipFinanceMirror:true, skipEcheanceAllocation:true)` so the move's `payment_state`/`amount_residual`/reconciliation track the devis-side échéance payments. `POST /devis/:id/remettre-brouillon` now also unwinds the linked invoice: deletes its reconciled payment `account_move`s + `account_payment` rows + `Facture` finance entries, deletes the invoice move (lines/tax-links/partials cascade), prunes orphan `account_full_reconcile`, nulls `devis.move_id` (the consumed journal number is not reclaimed — a sequence gap is accepted for this pre-prod undo). `getDevisComplet` returns `move: { id, name, state, paymentState, amountResidual, amountTotal } | null`. Frontend: a smart-button «Facture INV/… · <état paiement>» in the devis detail modal jumping to the Factures tab (`devis.voirFacture` i18n). Migration rehearsed ×2. `devis.test.js` +3 (facturer complet → linked posted balanced move settled `paid`; échelonné + acompte → move `not_paid` then échéance payment → devis `Payé partiellement` + move `partial` residual 7000; remettre-brouillon → move 404). **130 → 133 integration tests / 22 files.**
- **ERP "Comptabilité" roadmap — Stage 3 (`account.move` + `account.move.line`, double-partie), 2026-08-30.** User chose **full double-entry** (chart posting + reconciliation), and **`account.move` standalone this stage** — `POST /devis/:id/facturer` is UNCHANGED (rewire deferred to Stage 3b/4). New tables (`migrate.js`, Odoo field names): **`account_move`** (`move_type` entry/out_invoice/out_refund/in_invoice/in_refund, `state` draft/posted/cancel, `name` NULL→`INV/2026/0001` at post, `partner_id`, `invoice_date`/`invoice_date_due`/`date`, `invoice_origin`, `payment_term_id`, `amount_untaxed`/`amount_tax`/`amount_total`/`amount_residual`, `payment_state` not_paid/partial/paid/…, `reversed_entry_id` self-FK for Stage 5; partial unique index on `(journal_id, name)` WHERE name IS NOT NULL), **`account_move_line`** (`display_type` product/line_section/line_note/tax/payment_term, `quantity`/`price_unit`/`discount`/`price_subtotal`/`price_total`, **`debit`/`credit`/`balance`/`account_id`**, `tax_line_id`, **`amount_residual`/`reconciled`/`full_reconcile_id`/`matching_number`**, `date_maturity`), **`account_move_line_taxes`** M2M, **`account_full_reconcile`** (`name` = matching number `A00001`), **`account_partial_reconcile`** (`debit_move_line_id`/`credit_move_line_id`/`amount`/`full_reconcile_id`), **`account_payment`** (delegates to its own `account_move`). `echeances_paiement` gained a nullable `move_id` and `devis_id` was relaxed to nullable. **`partner_id` FKs to `contacts` are added by a deferred `DO $$` block after `contacts` is created** — same reason as `devis.client_id` (the account_* tables are created earlier in the single SQL template than `contacts`); a from-scratch DB 500'd until this was fixed (found by the integration `globalSetup`). **Post generates the balanced entry**: receivable (`asset_receivable`/`121000`) D=total; income (journal default or `income`/`400000`) C=subtotal per line; one tax line per tax (`liability_current`/`251000`) C=tax; Σdebit=Σcredit asserted (else 400); `name` from `prochainNumeroJournal` (Stage 2). `out_refund` flips signs (prefix `RINV/…`). **`POST /api/factures/:id/register-payment`** creates an `account_payment` + its journal entry (bank/cash D ↔ receivable C), an `account_partial_reconcile` against the invoice's receivable line, recomputes both lines' `amount_residual`, and on full settlement creates an `account_full_reconcile` + stamps `matching_number` — then updates `move.amount_residual`/`payment_state`, marks covered `echeances_paiement`, and mirrors to `finances` via new `syncFacturePaiement` (`source_module='Facture'`). Routes `/api/factures`: GET list (`?moveType=&state=&partnerId=`) + GET `:id` (move + lines + taxes + échéances + paiements), POST/PUT (draft only), `POST :id/post` / `:id/button-draft` (posted→draft only if `not_paid`) / `:id/cancel` / `:id/register-payment` (all `requireRole('admin','directeur')`), DELETE (draft/cancel only, Odoo rule). Frontend: **new `src/components/FacturesModule.jsx`** (list + create-draft form with `TaxSelect` + detail modal showing accounting lines/échéances/paiements + post/cancel/back-to-draft/delete/register-payment), nav tab «Factures» gated on the `finances` permission + `activated.finances`. `taxesLigneCalc` lifted from `App.jsx` into **`src/lib/taxes.js`** (shared with `DevisModule`). i18n `factures.*` + `nav.factures` + `common.all` fr/en. Migration rehearsed ×2 on restored `agri_app` (idempotent, devis/échéances counts unchanged). `factures.test.js` (10 tests: draft→post balance/numbering, partial→partial+`account_partial_reconcile`, settle→paid+`matching_number`, button-draft rules, delete rules, `out_refund` sign flip, role gate, tenant isolation).
- **ERP "Comptabilité" roadmap — Stage 2 (`account.journal` + `account.account` + séquences), 2026-08-30.** Configuration objects only — **no `account.move`, no ledger posting, no change to devis/factures or `finances`**; the "full double-entry vs document-model-on-cash-basis" call stays deferred to Stage 3's entry. New tables (Odoo field names): **`account_account`** (`code`, `name`, `account_type` = the 19-value Odoo selection via CHECK, `reconcile`, `active`, `UNIQUE(entreprise_id, code)`), **`account_journal`** (`name`, `code` ≤5 chars, `type` sale/purchase/cash/bank/general, `sequence`, `refund_sequence`, `default_account_id` FK `ON DELETE SET NULL`, `UNIQUE(entreprise_id, code)`), **`account_journal_sequence`** (`journal_id`, `prefix`, `last_number`, `UNIQUE(journal_id, prefix)`) — the per-journal counter. Seeded per entreprise at `register` + by `migrate.js:seedComptaConfigForExistingEntreprises` from `server/src/utils/comptaDefauts.js` (`COMPTES_DEFAUT` = generic chart codes `121000`/`211000`/`101401`/`101402`/`400000`/`500000`/`251000`/`131000` — **not a national PCG**, global scope; `JOURNAUX_DEFAUT` = INV/BILL/BNK/CSH/MISC). **`server/src/utils/journalSequence.js:prochainNumeroJournal(client, journalId, entrepriseId, date, {refund})`** produces `CODE/YYYY/NNNN` (or `RCODE/YYYY/NNNN` for a refund when the journal has `refund_sequence`) — year in the prefix ⇒ automatic yearly reset; increments under a row lock in the caller's transaction; **Stage 3's `account.move` will consume it** (nothing calls it yet). Routes `/api/journals` + `/api/accounts` (GET open, writes `requireRole('admin','directeur')` — same shape as `/api/taxes`). Frontend: a collapsible `ComptaConfigPanel` référentiel in `DevisModule` (journaux + comptes, add/delete). i18n `comptaConfig.*` fr/en. Migration rehearsed ×2 on a restored `agri_app` copy (idempotent; 9×8 comptes / 9×5 journaux seeded; devis totals unchanged).
- **ERP "Comptabilité" roadmap — Stage 1 (`account.tax`), 2026-08-30.** The old per-line `devis_lignes.taux_taxe` (a bare `%`) is **retired** (migration drops the column; `migrate.js:migrateTaxeDevisLignesVersAccountTax` converts any non-zero row into an `account_tax` `percent` named `TVA {taux} %` + links via the junction — 0 rows in prod, effectively a no-op). New: **`account_tax`** (Odoo field names: `type_tax_use` sale/purchase/none, `amount_type` percent/fixed/group/division, `amount` NUMERIC(16,4), `price_include`, `include_base_amount`, `active`, `sequence`, `description`, `invoice_label`, `UNIQUE(entreprise_id, name)`) + **`devis_lignes_taxes`** junction (Many2many, like `sale.order.line.tax_id`). Stage 1 **computes** `percent` and `fixed` (per-unit) only, plus `price_include` (base extraction) and `include_base_amount` (cascading); `group`/`division` pass the CHECK but compute to 0 (deferred to `account.move` repartition lines). CRUD at `/api/taxes` (writes gated `requireRole('admin','directeur')`), **no default taxes seeded** (global scope — no hardcoded country VAT). `calculerTotal` in `routes/devis.js` recomputes from the linked `account_tax` rows via the shared `utils/taxeCompute.js:appliquerTaxesLigne` (also used by `devisPdf.js` for a HT / per-tax / TTC summary block + a "Taxes" column). `devis.js` `POST`/`PUT` accept `lignes[].taxIds` (validated against the caller's entreprise — a foreign/bogus id is silently dropped); `getDevisComplet` returns aggregated `taxIds` per ligne + a `taxes` referentiel array on the devis. Frontend: per-ligne number input replaced by a `TaxSelect` multi-checkbox picker, a collapsible `TaxesPanel` référentiel, client-side total recompute mirrors the backend (`taxesLigneCalc` in `App.jsx`), i18n `taxes.*` fr/en. Migration rehearsed ×2 on a restored `agri_app` copy (idempotent, devis totals unchanged).

### Database migrations
`server/src/db/migrate.js` is now the **single** source of schema truth — one large, hand-maintained, idempotent script (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`), not versioned beyond this one file. `migrations/001_create_observations_table.sql` at the repo root used to be a second, unconnected mechanism (plain SQL, no runner, had to be applied by hand) — its `CREATE TABLE observations` was folded into `migrate.js` 2026-08-13, so that file is now superseded/historical only; don't apply it separately, everything runs through `migrate.js`.

**Drift risk — the dev DB can be ahead of `migrate.js`.** Because the running `agri_app` DB has accreted columns over time, a route can work locally against a column that `migrate.js` never actually creates, so a *fresh* `docker-compose up` (or the integration-test DB) is broken there. Found 2026-08-30 via the integration suite: `parcelles_historique`, `cultures_mouvements`, `poulailler_mouvements`, `poulailler_livraisons`, `poulailler_suivi` all query/insert `entreprise_id` but their `CREATE TABLE` bodies lacked it and there was no `ADD COLUMN IF NOT EXISTS entreprise_id` — dev had the column from earlier hand-drift, fresh installs 500'd on every cultures/poulailler mouvement/historique/livraison/suivi write. Fixed by adding the 5 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS entreprise_id` + backfill + indexes. Lesson: when adding a route that filters by a column, grep `migrate.js` to confirm that column is actually *created* there, not just assumed to exist because the dev DB has it. The integration suite (which migrates a throwaway DB from scratch each run) is now the guard against this.

Running migrations from the host (rather than `docker exec agri-app-backend-1 node src/db/migrate.js`) requires pointing at the Docker `db` container's host port explicitly (`5433`, see below) — `server/.env`'s `DB_PORT` must stay `5432`, since that's what the **backend container** needs internally to reach `db:5432` over the Compose network (host-side port remapping never affects container-to-container traffic). `docker exec` remains the simplest correct way to run migrations and is what's documented in Commands above.

### Local dev environment on this machine — Docker Desktop, port layout, and a resolved port collision
The app normally runs as 4 Docker Compose services (`docker-compose up --build`):

| Service | Image/build | Host port | Notes |
|---|---|---|---|
| `db` | `postgres:18-alpine` | **`5433:5432`** | app data lives here (host port intentionally *not* 5432 — see below) |
| `backend` | `./server` (Dockerfile) | `4000:4000` | `DB_HOST` overridden to `db` (Compose network) in `docker-compose.yml`; `DB_PORT` stays `5432` (container-internal, from `server/.env`) — these are independent of the host-side `5433` mapping |
| `frontend` | root `Dockerfile` (Vite build → nginx) | `8090:80` | `VITE_API_URL` baked in at build time |
| `backup` | `server/Dockerfile.backup` | *(none published)* | `pg_dump` every 6h, see Backups above |

**There is also a native Windows PostgreSQL service (`postgres.exe`) permanently listening on port 5432 on this machine, independent of Docker** — confirmed by querying it directly (a different, smaller dataset than the app's real data). `db`'s host port was deliberately moved to **`5433`** (2026-08-12) to stop colliding with it. Consequences:
- **pgAdmin** (or any host-side tool/script) must connect to `localhost:5433` for the real app database — `localhost:5432` is now unambiguously the native Windows Postgres, not Docker's.
- Container-to-container traffic (backend → db) is unaffected either way — it always uses the container-internal port 5432 regardless of the host mapping, which is why no other file needed to change for this fix.

### Backups
`server/Dockerfile.backup` + `server/backup.sh`: a sidecar container loops forever, running `pg_dump` every 6 hours into `/backups`, keeping only the 14 most recent dumps. `backups/` and `agri_app_backup.dump` at the repo root (both gitignored via `*.dump`/`backups/`) are artifacts of this/manual dumps, not something the app reads at runtime — corrected 2026-08-16, this file was never actually committed despite an earlier note here claiming otherwise.

**Verified restorable (2026-08-12)**: restored the latest dump into a throwaway database (`pg_restore` inside the `db` container) and confirmed row/table counts matched the live DB exactly. The restore mechanics work.

**Found and fixed (2026-08-12): silent empty-dump failures.** 3 of the last 14 rotated dumps were 0 bytes — `backup.sh` didn't check `pg_dump`'s exit code robustly (`set -e` alone wasn't catching it in practice) or verify the output file was non-empty, so failed backups were silently kept and rotated in as if valid. Likely cause: `backup`'s `depends_on: - db` (old short form) only waited for the `db` *container* to start, not for Postgres inside it to actually be ready — a classic cold-start race. Fixed:
- `docker-compose.yml`: `db` now has a `healthcheck` (`pg_isready`); `backend` and `backup` now `depends_on: db: condition: service_healthy` instead of the old unconditional form.
- `server/backup.sh`: now explicitly checks `pg_dump`'s exit code and that the resulting file is non-empty (`[ -s "$BACKUP_FILE" ]`) before declaring success; deletes and fails loudly (`exit 1`) otherwise instead of leaving a useless 0-byte file in the rotation.
- The 3 pre-existing empty dumps were deleted from `backups/`.

**Alerting + automated restore-test added 2026-08-29** (`server/backup.sh`). Since this container is a bare `postgres:18-alpine` (no node → no access to the app `mailer`), "alerting" is **sentinel files** in the host-mounted `./backups/`, meant to be watched by an operator or a future check:
- `.last_success` — rewritten (timestamp + size) on every successful dump.
- `.last_failure` — written (timestamp + reason) whenever `pg_dump` fails or produces an empty file; **removed** on the next successful dump. Its presence = a dump is currently broken.
- `.last_restore_test` — result of the last restore test, `"<ts> OK …"` or `"<ts> FAIL …"`. Not written to `.last_failure` (which a later good dump would wipe, masking it) — a leading `FAIL` here is its own alert.
- Restore test: runs at most once per `RESTORE_TEST_INTERVAL_HOURS` (env, default 168 ≈ weekly; checked via `find -mmin` on `.last_restore_test`), **after** the dump+rotation and never blocking them (`run_restore_test || true`). It `createdb agri_app_restore_test` → `pg_restore` the newest dump → compares table count (schema `public`) + `users` + `entreprises` row counts against the live DB → `dropdb`. `rc != 0` from `pg_restore` **or** any count mismatch → `FAIL`. `DB_USER` is the Postgres superuser (`POSTGRES_USER`), so create/drop are allowed.
- Verified end-to-end against the local stack: normal cycle writes `.last_success`; forced restore test passes (`tables 54/54 users 13/13 entreprises 9/9 rc=0`) and drops the throwaway DB; a simulated `pg_dump` failure (bad `DB_NAME`) writes `.last_failure` + `exit 1` without touching `.last_success`; the next good cycle clears `.last_failure`.

Still not done: turning the sentinel files into a real push alert (email/webhook) — deferred until hosting + SMTP exist; the files are the interim mechanism. `Dockerfile.backup` and `docker-compose.yml` unchanged (the 6h loop and env are the same; `RESTORE_TEST_INTERVAL_HOURS` can be set on the `backup` service if the weekly default needs tuning).

### Frontend (`src/`)
- `App.jsx` (**~7500 lines as of 2026-08-30**, up from ~4600 — grew with the ERP-UX layer, i18n `t(...)` calls, devis Kanban and the RH surface) is a single-file app containing most views, state, and async data-loading logic — mirrors the backend's flat-file style. Prefer small, targeted edits over broad rewrites; extraction into `src/components/`/`src/modules/` (e.g. `finances.jsx`, `ObservationListView.jsx`, `GlobalSearch.jsx`, `MonEspaceRh.jsx`, `RhReferentiels.jsx`, `EmployeeRhModal.jsx`, `PaymentTermsPanel.jsx`) is the existing pattern for pulling pieces out. **The line numbers in the component map below are from the ~4600-line era and are now badly off — `grep -n "^function \|^export function " src/App.jsx` to get real positions before editing.** Full component map (in file order — `App` is the default-exported root shell; `ProfilModule` is defined *after* `App` but referenced inside it, which works only because `function` declarations hoist):
  - `ParcelMapTab`, `EnvironnementTab` (`MovementTab` + its `renderInvoiceHtml` helper were **deleted 2026-08-29** — confirmed-dead code, never rendered since Ventes tabs switched to `VentesWithDevis`/`DevisModule`; ~470 lines removed)
  - 553 `DevisModule`, 1057 `VentesWithDevis`, 1087 `AchatModule`, 1450 `StocksTab`, 1666 `LivraisonsTab`, 1772 `ComptabiliteTab`, 1915 `PoultryMonitoringTab`
  - 2050 `CulturesModule`, 2325 `PoulaillerModule`, 2368 `LoginScreen`, 2562 `OptionCard`, 2597 `AgriculturalCalendarModule`, 2801 `HarvestsModule`, 2950 `AIAssistantModule`, 3073 `ForecastingModule`, 3169 `ReportsModule`, 3304 `HomeOverview`
  - 3427 `EmployeesModule`, 3607 `NotificationsModule`, 3666 `ClientsModule`, 3852 `FournisseursModule`, 4024 `ModulesScreen`
  - 4100 `export default function App` (root shell: auth gate, tab navigation, wires all modules above — now also mounts `ObservationListView` as a plain, role-ungated `observations` tab, added 2026-08-13)
  - 4418 `ProfilModule`
  - Line numbers above drifted ~60-80 lines from an earlier snapshot of this map after 2026-08-13's fixes (Achats date bug, Observations tab wiring, Calendrier/Récoltes backend rewrite) — re-`grep` `^function ` if they drift again rather than trusting them blindly.
  - Styling is 100% inline (`style={{...}}`) driven by the module-level `COLORS` object (line ~37) plus a handful of `className` hooks (`spin`, `app-shell`, `topbar`, `nav-chip`, `dashboard-shell`) defined in `App.css` — there is no Tailwind/CSS-in-JS despite `src/package.json` (the stray, unused one) listing `tailwindcss` as a dependency. `App.css` is now actually imported (`import './App.css';`, line 1) — fixed 2026-08-13 as part of the recovery below; before that fix, **nothing imported it at all** (neither `App.jsx` nor `main.jsx`), so those className hooks had silently had no effect for who knows how long — worth an actual visual check next time the UI is open, since this may change how things look.
  - **Corruption incident + recovery (2026-08-13)**: this file has now been hit by the same corruption pattern documented below at least three times. This time it was severe enough to be unbuildable — while working on an unrelated task (adding named roles), the working-tree `App.jsx` was found at 5800 lines (vs. 3191 in `git HEAD`), missing `export default function App` and core shared components (`Button`, `Card`, `Field`, `Select`, `FinancesModule`, `notify`/`notifyError`/`notifySuccess`, etc.) entirely, with several components duplicated 3-4× (`EmployeesModule`, `ClientsModule`, `HomeOverview`, `NotificationsModule`), and truncated mid-statement at the very end. **Important discovery: `git HEAD` was not a safe restore target** — it turned out to be a much older, pre-feature snapshot (no Devis/Achats/Banques/MFA/Observations UI at all) that had never been updated, because `App.jsx` work has apparently never been committed at all — everything past that point only ever existed as uncommitted working-tree state. Restoring from HEAD (done first, on request) was a real regression, just a less obviously-broken one than the 5800-line corruption. **What actually worked**: VS Code's local file history (`%APPDATA%\Code\User\History\<hash>\entries.json` + numbered snapshot files, hash `-4fa68af7` for this file) had a snapshot from 2026-08-10 (`KHQl.jsx`, 4599 lines, balanced braces, component structure matching this very map almost line-for-line) that was far more complete and was used as the real restore base — after stripping 2 stray meta-commentary lines pasted at the very top (same corruption pattern, smaller scale) and fixing one more small pocket of it around the `DevisModule` form's submit-button/total row (duplicated `<Button>`/`<div>` tags, an unclosed `<div ...>` tag, and two `className="card p-6 my-6"` Tailwind blocks containing raw French meta-commentary like "doit être ajusté" — all deleted, replaced with the single clean copy the surrounding pattern implied). Verified clean afterward: no duplicate top-level `function`/`const` declarations, no stray Tailwind classes beyond the legitimate `app-shell`, no leftover meta-commentary text, `npx vite build` succeeds, and the root `npm test` suite (`ObservationListView.test.jsx`, 6 tests) passes.
  - **Lesson for next time this recurs**: don't assume `git HEAD` is a safe recovery baseline for this file specifically — check whether the corrupted state is actually *ahead* of HEAD in features first (compare component lists, like this entry did) before restoring over it. VS Code's per-file local history (independent of git, keyed by a hash of the file path under `%APPDATA%\Code\User\History\`) is a real recovery option when it exists and git doesn't have what's needed — check it before concluding recent work is unrecoverable. The tell for a fresh occurrence is unchanged: duplicated adjacent JSX blocks, French "reste du code doit être ajusté" style comments, or `className="..."` Tailwind utility classes (the rest of the file never uses Tailwind) — worth a quick skim of the surrounding region before trusting it at face value, and worth actually running a build (not just a syntax-only check) since this class of corruption doesn't always trip a simple parser.
- `src/lib/api.js` is the sole HTTP client: a `fetch` wrapper (`request`) attaching the JWT bearer token from `localStorage`, centralizing 401 handling (dispatches `agri-auth-expired` to force logout).
- Offline support: `safeRequest` (in `api.js`) is used for writes that should survive being offline — on network failure it queues the operation into `localStorage` (`agri-offline-queue`) instead of throwing; `flushOfflineQueue` replays the queue on reconnect. Real server errors (4xx/5xx) are never queued, only network failures are. `src/utils/storage.js` layers a second, parallel local-first cache (`storageGet`/`storageSet` + `agri-sync-queue`) on top — two overlapping offline-queue mechanisms exist side by side (`agri-offline-queue` in `api.js` vs `agri-sync-queue` in `storage.js`); check which one a given piece of UI actually reads before adding to either.
- PWA/offline installability is configured via `vite-plugin-pwa` in `vite.config.js` (`VitePWA`, `registerType: 'autoUpdate'`).
- `VITE_API_URL` controls the API base URL at build time (default `http://localhost:4000/api`), passed as a Docker build arg in the root `Dockerfile`.

### Testing (two independent Jest setups — frontend and backend)
This is a two-project repo (`src/` and `server/` each have their own `package.json`/`node_modules`), and each now has its own Jest config; they must not be merged.
- Frontend (root): `babel.config.cjs` + `"jest": { "testEnvironment": "jsdom", "testPathIgnorePatterns": ["/node_modules/", "/server/"] }` in the root `package.json`. Without the `testPathIgnorePatterns` entry, root `npm test` would also walk into `server/` and try to run backend tests under the frontend's jsdom + `node_modules` — that crashes (backend deps like `pdfkit`/`fontkit` aren't set up for a browser-like jsdom environment). `@babel/preset-env`/`@babel/preset-react` and `jest-environment-jsdom`/`@testing-library/dom` are devDependencies for this.
  - `babel.config.cjs` has an `env.test` block with a tiny inline plugin that rewrites `import.meta` → `({})` — **jest only** (Vite handles `import.meta` natively and doesn't apply `env.test`). Needed so `src/lib/api.js` (whose first line reads `import.meta.env.VITE_API_URL`) can be `require`d under babel-jest instead of only ever being `jest.mock`'d. `api.js` line 1 is also guarded (`typeof import.meta !== 'undefined' && import.meta.env?.…`) so the fallback URL is explicit.
  - Coverage (87 tests / 9 files as of 2026-08-30). Logic suites: `src/components/roles.test.js` (`ROLE_DEFINITIONS` shape + `mapUiRoleToBackend`/`mapBackendRoleToUi` round-trips & fallbacks), `src/lib/locale.test.jsx` (`fmtMoney`/`fmtNumber`/`fmtDate` per currency+locale, blank→`—`, invalid-currency fallback, `useLocale` in/out of `LocaleProvider`), `src/lib/api.test.js` (token helpers, `request` 401→logout event, network→friendly error, `safeRequest` offline-queue vs real-error rethrow, `flushOfflineQueue` replay/partial-failure), `src/utils/storage.test.js` (`storageGet` fallback, `storageSet` offline queueing, `syncPendingChanges`). Component suites: `ObservationListView.test.jsx` (the original), `ui.test.jsx` (`Card` forwards `onClick`/DOM props — the documented regression; `Button` type/disabled/variants; `Field`/`Select` label + prop forwarding + `AideChamp` on `aide`; `Badge` fallback; `MiniChart` empty/bars; toast show/dismiss), `FeedbackModule.test.jsx` (submit validation + `createFeedback` + reset; platform-admin list load/error/status-change), `GlobalSearch.test.jsx` (min-2-chars gate, debounced `rechercheGlobale`, grouped results, `onSelect`/`onClose`), `RhReferentiels.test.jsx` (`canManage` gate, lazy-load on expand, add/delete département). Pattern for API-touching components: `jest.mock('../lib/api.js')` + `i18n.changeLanguage('fr')` in `beforeAll`. Remaining gap: the big `App.jsx` module views (`EmployeesModule`, `DevisModule`, `CulturesModule`, …) — not extracted, not directly testable.
- Backend (`server/`) has **two** Jest configs:
  - **Unit** — `npm test` (`server/babel.config.cjs`, `@babel/preset-env` only). The `"jest"` block in `server/package.json` has `testPathIgnorePatterns: ["/node_modules/", "/src/test/integration/"]` so it stays DB-free and fast. Only test today: `server/src/test/devisPdf.test.js`.
  - **Integration** — `npm run test:integration` (`server/jest.integration.config.cjs`, added 2026-08-29). Runs `supertest` against the real Express app (`server/src/app.js`, the shared app factory `server.js` also imports) with **native ESM** (`node --experimental-vm-modules …`, `transform: {}`) — not babel — because `auth.js`/`mfa.js` were reworked from `createRequire('otplib')` to plain `import` (otplib 13 is dual ESM/CJS; the CJS path pulls in `@scure/base` which Jest's CJS runtime can't load). `globalSetup` drops+recreates+migrates a dedicated `agri_app_test` DB **on the Docker `db` container (host port 5433)** — never the dev DB; `globalTeardown` drops it (`TEST_DB_KEEP=1` to keep it for post-mortem). Config/creds: `src/test/integration/testDb.cjs` reads `DB_USER`/`DB_PASSWORD`/`JWT_SECRET` from `server/.env`, forces host/port/name to the test DB; `env.js` (setupFiles) blanks `EMAIL_*` (so mail sends fail as expected — asserted). Prereq: `docker compose up -d db`. Runs with `--forceExit` (a stray keep-alive handle otherwise hangs jest after the pools close). Coverage (154 tests / 26 files as of 2026-08-30) — incl. `paymentTerms.test.js` (seed + CRUD + role gate + tenant isolation), `taxes.test.js` (CRUD, no-seed, `amount_type` validation, 409 on dup name, role gate, tenant isolation), `journals.test.js` + `accounts.test.js` (Stage 2: seed check, CRUD, `code`/`account_type` validation, 409 on dup, `defaultAccountId` cross-tenant → 400, role gate, tenant isolation, plus `prochainNumeroJournal` — NNNN increment, yearly reset, `RCODE` refund prefix, cross-tenant → throw), `factures.test.js` (Stage 3: draft→post Σdebit=Σcredit + receivable=total + income=untaxed + tax lines + `INV/YYYY/NNNN` numbering, post refused without partner/product line/if already posted, `register-payment` partial → `partial` + `account_partial_reconcile` + settle → `paid` + `matching_number`, `button-draft` rules, DELETE draft/cancel-only, `out_refund` sign flip, admin/directeur write gate, tenant isolation) and Stage-0/Stage-1 additions to `devis.test.js` (validity/expired, delete-Annulé, `facturer` with `paymentTermId`/acompte; `taxIds` on lignes → total with percent/fixed/price_include/include_base_amount/two-taxes/global-remise-before-tax, foreign taxId silently ignored, PUT recompute): `auth.test.js` (register/login/roles/tenant isolation), `devis.test.js` (full quote→invoice→installment→revert lifecycle + audit-log assertions), `mfa.test.js` (TOTP + email 2FA setup/verify + 2-step login), `achats.test.js` (purchase doc brouillon→commandé→reçu→réception annulée, the finance entry it creates/removes, transition guards, tenant isolation), `rh.test.js` (`/api/rh` référentiels + role gate, congé self-service request → can't self-approve → admin approves → `conges-solde` decrements, avances admin-only, contracts one-active invariant, tenant isolation), `contacts.test.js` (client/fournisseur asymmetry + `?type=` filters, company/sub-contacts, tags, delete-with-devis → 409, tenant isolation), `listesPrix.test.js` (list CRUD + line upsert + validation, assign to contact → `prix-effectifs`, `ON DELETE SET NULL` detach, tenant isolation), `banques.test.js` (CRUD, admin/directeur write gate, principal account → achat finance entry categorized `Banque`, tenant isolation), `equipements.test.js` (CRUD + admin/directeur/gestionnaire write gate, maintenance sub-resource, tenant isolation incl. sub-routes), `observations.test.js` (CRUD + partial PUT, role-open POST, tenant isolation), `recoltes.test.js` (full CRUD since 2026-08-30 — `POST`/`PUT` required-field validation, `quantite: 0` accepted, `parcelleId` ownership validation → foreign/bogus stored as `null`, `PUT`/`DELETE` 404 on missing/cross-tenant), `calendar.test.js` (GET/POST/PUT only — required fields, partial PUT, role-open POST, date-sorted list, tenant isolation), `feedback.test.js` (open submission + `type` fallback to «Autre», `GET`/`PATCH` platform-admin-only → 403 for a normal admin, platform admin reads across all entreprises + triage), `planning.test.js` (`POST` returns 200 + a real per-culture plan sorted by date, persisted as `activites` — see the 2026-09-05 fix described under `services/` above; `cultureId` = a `parcelles.id`, foreign/bogus → 404), `cultures.test.js` (parcelles CRUD, vanne historique with ownership check, vente/achat mouvements ↔ `finances` sync on create/update/delete + `mouvement_historique` reason trail, `?type=` filter, tenant isolation), `poulailler.test.js` (mouvements ↔ `finances` sync, livraisons lifecycle, suivi quotidien, tenant isolation), `business.test.js` (manual finance entries — Caisse default / Banque needs `banqueId`, admin/directeur write gate, delete, tenant isolation). Helpers in `src/test/integration/helpers.js` (`registerEntreprise`, `createEmployeeLogin` → returns `salarieId`, `createClient`, `createProduit`, `createParcelle`).
- Fixes found by these tests (2026-08-30): (1) the `migrate.js` `entreprise_id` drift above; (2) `DELETE /api/cultures/parcelles/:id`, `DELETE /api/poulailler/livraisons/:id`, `DELETE /api/business/finances/:id` all returned `{ success: true }` / 200 on 0 rows (wrong tenant / bogus id) — now 404, same fix as `contacts`/`banques` before them.
- Fix found by `recoltes.test.js` (2026-08-30): `RECOLTE_COLUMNS` returned `quantite` uncast → JSON string `"1200.00"` instead of a number, unlike every other `*_COLUMNS` in the codebase (all `::float8`). The frontend already worked around it with `Number(...)` everywhere; now `quantite::float8 AS quantite`, consistent.
- Bug found + fixed by `contacts.test.js` (2026-08-30): `DELETE /api/contacts/:id` was scoped by `entreprise_id` (so no cross-tenant deletion) but never checked `rowCount`, returning `{ success: true }` / 200 even when 0 rows matched (wrong tenant or bogus id) — inconsistent with `PUT /:id` and every other module's DELETE. Now 404 on 0 rows.
- `npm install` in the frontend project needs `--legacy-peer-deps` at the root (see `Dockerfile`, which already does this) — `vite-plugin-pwa@0.9.3` declares a `vite@^2` peer while the project uses `vite@^5`. `server/` installs cleanly without it.

## Known issues / stray or broken code

**Repo-wide dead-file cleanup done 2026-08-16** (user-requested architecture audit). Everything below was investigated (checked for real imports, not just assumed dead) and removed — this section used to list them as known clutter; now it's a record of what was found and why it was safe to delete, in case similar drift recurs:
- `backend/routes/employees.js` / `backend/routes/mfa.js` — not stubs, real business logic, but **broken**: imported `../db.js`/`../middleware/auth.js` relative to `backend/`, and neither `backend/db.js` nor `backend/middleware/` ever existed. Never mounted in `server.js`, never imported from anywhere else (confirmed via a full-repo grep excluding `node_modules`). The real, live equivalents are `server/src/routes/salaries.js` and `server/src/routes/mfa.js` — the risk this cleanup removed was a future edit landing in the dead file with the confusingly similar name/purpose instead of the real one.
- `frontend/` — empty directory, no tracked files.
- `server/src/app.js` — was deleted here as a stale duplicate of `server.js`; **recreated 2026-08-29** with a genuinely different role: the shared Express app factory (all `/api/*` route mounting), imported by both `server.js` (which now only does `testDatabase()` + `listen()`) and the integration test suite (supertest, no listener). Not dead code anymore.
- `server/src/data/store.js` + `server/data/app-data.json` — an orphaned pre-Postgres file-based persistence prototype (`readStore`/`writeStore` over a JSON file), confirmed unimported anywhere; the JSON file itself held one fake `admin@agriapp.com` test account, unrelated to the real app.
- `HEAD_App.jsx` / `HEAD_App_head.txt` — a frozen 3192-line snapshot from the merge-conflict/corruption recovery documented above, stale since (today's real `App.jsx` is 5000+ lines) — kept as-is for a while as a just-in-case reference, but long past the point that made sense.
- `src/package.json` — stray duplicate of the root `package.json` from an earlier reorg (declared `tailwindcss`, which the app has never used).
- `src/assets/react.svg` / `src/assets/vite.svg` — original Vite template assets, confirmed unreferenced anywhere in `src/`.
- `migrations/001_create_observations_table.sql` — fully superseded since 2026-08-13 (its `CREATE TABLE observations` was folded into `server/src/db/migrate.js`), kept around historically until now; genuinely nothing left pointing to it.
- `.continue/prompts/new-prompt.md` — a Continue.dev (IDE autocomplete extension) config file, personal editor tooling rather than project code — shouldn't have been committed in the first place.
- `agri_app_backup.dump` at the repo root — investigated and turned out to be a **non-issue**: this file exists on disk but was never actually git-tracked (`*.dump` in `.gitignore` correctly caught it) — the note that used to live here claiming it was committed was simply wrong; corrected rather than "fixed" since there was nothing to fix.
